前端开发里，**惰性函数（Lazy Function / Lazy Initialization）**是一个非常值得掌握的技巧。

它表面上只是：

> **第一次调用时做一次判断、初始化或计算，后面直接走已经确定好的逻辑。**

但再往深一点看，它其实体现了前端非常重要的一种思想：

> **把“运行时判断”尽可能前移到“初始化阶段”，让后续执行路径变得更简单。**

---

# 一、先理解什么叫“惰性函数”

最经典的例子：

```js
function addEvent(element, type, handler) {
  if (element.addEventListener) {
    element.addEventListener(type, handler)
  } else if (element.attachEvent) {
    element.attachEvent('on' + type, handler)
  }
}
```

假设这个函数被调用 10000 次。

那么每一次调用都要判断：

```js
if (element.addEventListener) {
  ...
} else {
  ...
}
```

实际上：

**浏览器环境通常不会在第一次调用之后突然改变。**

所以第一次判断出：

```js
element.addEventListener
```

存在以后，后面 9999 次根本没有必要再判断。

于是可以写成：

```js
let addEvent

if (window.addEventListener) {
  addEvent = function (element, type, handler) {
    element.addEventListener(type, handler)
  }
} else {
  addEvent = function (element, type, handler) {
    element.attachEvent('on' + type, handler)
  }
}
```

之后：

```js
addEvent(el, 'click', handler)
addEvent(el, 'click', handler)
addEvent(el, 'click', handler)
```

直接执行已经确定的函数。

这就是一种典型的**惰性函数 / 函数自重写思想**。

---

# 二、还有一种更经典的写法：第一次调用时“自我改写”

例如：

```js
function getUserAgent() {
  if (navigator.userAgent.includes('Chrome')) {
    getUserAgent = function () {
      return 'Chrome'
    }
  } else {
    getUserAgent = function () {
      return 'Other'
    }
  }

  return getUserAgent()
}
```

第一次：

```js
getUserAgent()
```

执行：

```text
getUserAgent
    ↓
判断环境
    ↓
发现 Chrome
    ↓
重新定义 getUserAgent
    ↓
执行 Chrome 版本
```

第二次：

```js
getUserAgent()
```

已经变成：

```js
function () {
  return 'Chrome'
}
```

不会再进行判断。

所以它实际上形成：

```text
第一次：

调用
 ↓
判断
 ↓
初始化
 ↓
替换自己
 ↓
执行


以后：

调用
 ↓
直接执行
```

这就是“惰性函数”非常核心的思想。

---

# 三、为什么叫“惰性”？

因为它不是一开始就把所有事情做完。

例如：

```js
const config = expensiveCalculation()
```

程序启动时就计算。

而惰性方式：

```js
let config

function getConfig() {
  if (!config) {
    config = expensiveCalculation()
  }

  return config
}
```

变成：

```text
程序启动
    ↓
什么都不做

第一次 getConfig()
    ↓
真正计算
    ↓
缓存

第二次 getConfig()
    ↓
直接返回缓存
```

因此：

**Lazy = 延迟执行 + 必要时初始化 + 后续复用。**

---

# 四、惰性函数和“懒加载”有什么关系？

两者思想非常接近，但不是完全相同。

### Lazy Function

强调：

> **函数第一次真正执行的时候才确定自己的实现。**

例如：

```js
function getStorage() {
  // 第一次调用才初始化
}
```

---

### Lazy Loading

强调：

> **资源第一次真正需要的时候才加载。**

例如：

```js
const module = await import('./heavy-module.js')
```

Vue：

```js
const UserPage = defineAsyncComponent(
  () => import('./UserPage.vue')
)
```

路由：

```js
{
  path: '/users',
  component: () => import('./views/User.vue')
}
```

它们背后其实都是：

```text
不要提前做
      ↓
真正需要的时候再做
```

所以你可以把：

**惰性函数、懒加载、Lazy Initialization、Memoization**

放在一个大的思想体系里面理解：

> **延迟成本，把成本推迟到真正需要的时候。**

---

# 五、前端里一个非常实用的例子：事件监听能力检测

比如兼容不同环境：

```js
let addEventListener

function init() {
  if (window.addEventListener) {
    addEventListener = function (el, type, handler) {
      el.addEventListener(type, handler)
    }
  } else {
    addEventListener = function (el, type, handler) {
      el.attachEvent('on' + type, handler)
    }
  }
}
```

更进一步，可以让函数第一次调用时自己初始化：

```js
function addEvent(el, type, handler) {
  if (el.addEventListener) {
    addEvent = function (el, type, handler) {
      el.addEventListener(type, handler)
    }
  } else {
    addEvent = function (el, type, handler) {
      el.attachEvent('on' + type, handler)
    }
  }

  return addEvent(el, type, handler)
}
```

第一次：

```text
addEvent()
 ↓
能力检测
 ↓
重写 addEvent
 ↓
执行
```

以后：

```text
addEvent()
 ↓
直接执行
```

这就是：

> **函数第一次执行时完成自身特化。**

---

# 六、现代前端里更常见的是“惰性初始化”

这个比“函数自重写”更加实用。

例如：

```js
let client

function getClient() {
  if (!client) {
    client = createClient()
  }

  return client
}
```

第一次：

```js
getClient()
```

执行：

```text
client === undefined
       ↓
createClient()
       ↓
保存
```

第二次：

```js
getClient()
```

执行：

```text
client 已存在
       ↓
直接返回
```

这其实就是：

**Lazy Singleton**

也就是：

> **惰性初始化 + 单例。**

---

# 七、前端项目里到处都是这种东西

比如 Supabase：

```js
let supabase

export function getSupabase() {
  if (!supabase) {
    supabase = createClient(
      SUPABASE_URL,
      SUPABASE_KEY
    )
  }

  return supabase
}
```

这非常适合：

```js
const client = getSupabase()
```

而不是应用启动的时候无条件创建所有东西。

---

# 八、Vue 项目里的典型场景

例如一个很重的第三方库：

```js
let chartInstance

async function getChart() {
  if (!chartInstance) {
    const module = await import('echarts')
    chartInstance = module
  }

  return chartInstance
}
```

第一次：

```js
await getChart()
```

发生：

```text
getChart
   ↓
没有 ECharts
   ↓
import('echarts')
   ↓
下载 JS
   ↓
初始化
   ↓
缓存
```

以后：

```js
await getChart()
```

变成：

```text
getChart
   ↓
已经存在
   ↓
直接返回
```

这就是：

> **Lazy Loading + Lazy Initialization + Cache**

组合。

---

# 九、惰性函数还有一个非常漂亮的用途：环境检测

例如你写一个：

```js
function getDeviceType() {
  // 非常复杂的判断
}
```

可能每次都判断：

```js
navigator.userAgent
window.innerWidth
touch support
platform
...
```

实际上设备环境在当前页面生命周期里通常不会频繁改变。

于是：

```js
let getDeviceType

function initDeviceDetector() {
  const isMobile = /Android|iPhone/i.test(
    navigator.userAgent
  )

  if (isMobile) {
    getDeviceType = () => 'mobile'
  } else {
    getDeviceType = () => 'desktop'
  }
}

initDeviceDetector()
```

之后：

```js
getDeviceType()
```

就是极其简单的：

```js
() => 'desktop'
```

---

# 十、它和 Memoization 不一样

这两个概念非常容易混。

## Memoization

缓存：

> **同一个输入 → 同一个输出**

例如：

```js
const cache = new Map()

function fibonacci(n) {
  if (cache.has(n)) {
    return cache.get(n)
  }

  const result = ...
  cache.set(n, result)

  return result
}
```

这里缓存的是：

```text
输入 → 输出
```

---

## Lazy Function

缓存的是：

> **函数实现 / 初始化结果**

例如：

```js
let fn

function getFn() {
  if (!fn) {
    fn = createFunction()
  }

  return fn
}
```

这里缓存的是：

```text
初始化 → 函数
```

---

# 十一、再往深一点：惰性函数其实是一种“运行时特化”

这个概念非常有意思。

普通代码：

```js
function foo() {
  if (A) {
    doA()
  } else {
    doB()
  }
}
```

每次：

```text
foo()
 ↓
判断 A
 ↓
doA / doB
```

惰性版本：

```js
let foo

if (A) {
  foo = doA
} else {
  foo = doB
}
```

变成：

```text
初始化阶段
 ↓
判断 A
 ↓
确定 foo
 ↓

运行阶段
 ↓
foo()
```

也就是：

> **把条件分支从“每次执行”移动到了“初始化阶段”。**

这在计算机科学里面非常接近：

**Partial Evaluation（部分求值）**

和：

**Runtime Specialization（运行时特化）**

的思想。

---

# 十二、这对前端性能有什么意义？

假设：

```js
function render() {
  if (isDarkMode) {
    // ...
  } else {
    // ...
  }
}
```

如果 `isDarkMode` 在整个生命周期都不会改变，那么每次 render 都判断一次其实没有必要。

可以在初始化阶段确定：

```js
const renderTheme = isDarkMode
  ? renderDark
  : renderLight
```

然后：

```js
renderTheme()
```

这样：

```text
普通：

render
 ↓
判断
 ↓
判断
 ↓
判断
 ↓
执行


惰性/特化：

初始化
 ↓
判断一次
 ↓
确定实现

render
 ↓
直接执行
```

---

# 十三、但现代 JavaScript 里不要为了“性能”滥用

这一点非常重要。

很多人学到惰性函数以后容易产生：

> “判断少一次，所以一定更快。”

实际上不一定。

例如：

```js
if (foo) {
  bar()
}
```

这种判断成本极低。

你为了省一个判断，搞成：

```js
let barFn

function foo() {
  if (!barFn) {
    barFn = ...
  }

  return barFn()
}
```

代码反而更复杂。

因此现代前端里：

**惰性函数真正有价值的地方通常不是“省一个 if”。**

而是：

### 1. 重型初始化

```js
createEditor()
createChart()
createWorker()
createWebSocket()
```

---

### 2. 重型依赖

```js
import('monaco-editor')
import('echarts')
import('pdfjs')
```

---

### 3. 环境能力检测

```js
WebGL
WebGPU
IndexedDB
Service Worker
Clipboard API
```

---

### 4. 单例资源

```js
Supabase Client
WebSocket
Worker
AudioContext
```

---

### 5. 很少使用的功能

例如：

```text
打开高级设置
       ↓
才加载高级设置模块
```

---

# 十四、一个非常漂亮的前端例子：Lazy Worker

假设你的应用只有用户进行 Excel 导入的时候才需要 Worker。

普通做法：

```js
const worker = new Worker('./excel-worker.js')
```

页面一打开就创建。

惰性：

```js
let worker

function getWorker() {
  if (!worker) {
    worker = new Worker('./excel-worker.js')
  }

  return worker
}
```

然后：

```js
function importExcel(file) {
  const worker = getWorker()

  worker.postMessage(file)
}
```

于是：

```text
用户没有导入 Excel

→ 不创建 Worker


用户第一次导入

→ 创建 Worker
→ 执行任务


用户第二次导入

→ 复用 Worker
```

这就是非常标准的：

> **Lazy Initialization + Resource Reuse**

---

# 十五、再结合前端架构理解

实际上一个成熟的 Vue 应用里面，会有大量这样的模式：

```text
                 App
                  │
        ┌─────────┴─────────┐
        │                   │
     初始化               Lazy
        │                   │
   核心功能             非核心功能
        │                   │
   Router/Pinia          ECharts
   Auth                  Monaco
   UI                    PDF
   API                   Worker
                          │
                     真正需要时
                          ↓
                       加载
                          ↓
                       初始化
                          ↓
                       缓存
```

这就是现代 Web 应用非常重要的设计原则：

> **Core First, Optional Later**

核心能力提前准备。

非核心能力延迟准备。

---

# 十六、最后总结成一个公式

我会建议你把惰性函数记成：

```text
Lazy Function
=
延迟执行
+
首次初始化
+
确定实现
+
缓存结果
+
后续复用
```

最典型的代码：

```js
let resource

function getResource() {
  if (!resource) {
    resource = createResource()
  }

  return resource
}
```

而更高级的“函数自重写”：

```js
function foo() {
  if (condition) {
    foo = fastPath
  } else {
    foo = slowPath
  }

  return foo()
}
```

其核心思想只有一句话：

> **第一次调用负责“决定以后怎么做”，以后调用只负责“执行已经决定好的事情”。**

如果你继续往前端底层深入，**惰性函数其实可以继续和「函数柯里化、闭包、Memoization、单例、懒加载、动态 import、代码分割、缓存、JIT、V8 Hidden Class / Inline Cache」串起来理解**。这些东西放在一起看，会发现现代 JavaScript 性能优化背后有一条非常清晰的主线：**减少重复工作，让运行时越来越“确定”。**
